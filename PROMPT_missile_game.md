# 미사일 회피 게임 개선 요청 프롬프트

> 아래 `---` 사이의 내용을 통째로 복사해서 GPT / Gemini 에 붙여넣으세요.
> `lib/main.dart` 파일도 함께 첨부하면 정확도가 크게 올라갑니다.

---

## 역할

너는 Flutter/Dart 시니어 개발자다. 아래 프로젝트의 미니게임 하나를 개선해야 한다.
추측하지 말고, 내가 준 실측값과 현재 코드 구조를 그대로 근거로 삼아 수정안을 내놓아라.

## 프로젝트 컨텍스트

- Flutter 앱, 미니게임 모음집. **모든 코드가 `lib/main.dart` 단일 파일**에 있다 (약 2,400줄).
- 외부 게임 엔진/패키지를 쓰지 않는다. `CustomPainter` + `Timer.periodic(16ms)` 로 직접 렌더링·업데이트한다.
- **외부 패키지를 새로 추가하지 마라.** Flutter SDK 기본 위젯과 `dart:ui` 만 사용한다.
- 대상은 "미사일 회피" 게임이며 관련 클래스는 다음과 같다.
  - `Missile` — `position`(Offset), `velocity`(Offset), `angle`(double)
  - `CloudParticle` — 배경 구름
  - `MissileEscapeDifficulty` — `spawnInterval`, `missileSpeed`, `maxMissiles`
  - `MissileEscapeScreen` / `_MissileEscapeScreenState` — 게임 상태와 루프
  - `MissileGamePainter` — 실제 렌더링 (`paint`, `_drawCloud`, `_drawFallbackFighter`, `_drawMissile`)

### `_MissileEscapeScreenState` 의 현재 주요 필드

```dart
ui.Image? fighterImage;                                   // assets/fighterJet.png
Offset playerPosition = Offset.zero;
Size screenSize = Size.zero;
final Size playerRenderSize  = const Size(70, 90);
final Size playerHitboxSize  = const Size(45, 65);
final Size missileHitboxSize = const Size(12, 30);
List<Missile> missiles = [];
double elapsedTime = 0.0;
int score = 0;                                            // score = (elapsedTime * 10).toInt()
double timeSinceLastSpawn = 0.0;
```

### 현재 동작

- 조작: 화면 전체를 덮는 `GestureDetector(onPanUpdate: _handlePanUpdate)` 로, 아무 데나 드래그하면 기체가 그 델타만큼 이동한다.
- 미사일: 화면 4방향 바깥에서 생성되어 **생성 시점의 플레이어 위치를 향해 직선**으로 날아온다. 유도는 하지 않는다.
- 충돌: `Rect.overlaps` 로 사각형 대 사각형 판정. 맞으면 즉시 `_gameOver()`.
- 난이도: `_getDifficulty(double time)` 이 시간 구간 4개(`<15s`, `<30s`, `<60s`, 그 이후)로 **하드코딩된 계단식** 값을 반환한다.
  현재 값: `spawnInterval` 1.5 → 1.0 → 0.7 → 최소 0.3, `maxMissiles` 6 → 12 → 18 → 최대 35.
- 상단 HUD 와 버튼은 `_glassChip()` 헬퍼(반투명 검정 + `BackdropFilter` 블러 + 흰색 테두리)로 만들어져 있다. **새로 만드는 UI도 이 스타일에 맞춰라.**

---

## 요구사항 1 — 전투기가 어두운 네모로 보이는 문제

### 원인 (이미 내가 확인한 사실이다. 이 전제로 작업해라)

`assets/fighterJet.png` 는 전투기만 오려낸 이미지가 **아니다**. 게임 HUD 화면을 통째로 캡처한 것 같은 이미지다.

| 항목 | 실측값 |
|---|---|
| 이미지 크기 | **1280 × 714** |
| 알파 채널 | **없음** (모든 픽셀 alpha = 255) |
| 배경 | 어두운 남색 `RGB(18, 30, 49)` + 청사진 격자무늬 |
| 배경에 포함된 잡요소 | 좌상단 체력바, 우상단 레이더 다이얼, 좌하단 게이지 2개 |
| **전투기 실제 영역** | **x 398 ~ 881 (폭 484), y 45 ~ 641 (높이 597)** |
| 전투기 가로세로비 | 약 **0.81** (w/h) |
| 기체 몸통 색 | `RGB(51,60,72)` ~ `RGB(106,115,127)` — 상당히 어둡다 |

그런데 `MissileGamePainter.paint()` 는 srcRect 에 **이미지 전체**를 넣고 있다:

```dart
canvas.drawImageRect(
  fighterImage!,
  Rect.fromLTWH(0, 0, fighterImage!.width.toDouble(), fighterImage!.height.toDouble()), // ← 전체
  playerRect,
  Paint()..filterQuality = FilterQuality.medium,
);
```

그래서 1280×714 HUD 화면 전체가 70×90 사각형으로 축소되어, **어두운 남색 네모 안에 아주 작은 전투기**가 들어간 모습으로 렌더링된다. 이것이 "네모로 나타나고 색이 어둡다"의 정체다.

### 원하는 결과

전투기 실루엣만 배경 없이, **밝고 눈에 잘 띄는 색**으로 보일 것.

### 두 가지 방안을 모두 제시하고, 각각의 완성된 코드를 달라. 그리고 어느 쪽을 권하는지 이유와 함께 말해라.

**방안 A — 에셋을 잘라 쓰는 방법**

1. `srcRect` 를 전투기 bbox 로 교체한다. 가장자리 여유 몇 px 포함: `Rect.fromLTWH(394, 41, 492, 605)`.
2. `playerRenderSize` 를 실제 비율 0.81 에 맞춘다 (예: `Size(74, 91)`). 현재 `Size(70, 90)` 은 비율이 0.78 이라 살짝 눌린다.
3. 배경 남색을 투명하게 만든다. **단순 색상 임계값(chroma key)은 쓰지 마라** — 기체 아랫부분의 어두운 회색이 배경 남색과 밝기·색상이 너무 비슷해서, 임계값으로 자르면 기체에 구멍이 뚫린다. 대신 **이미지 테두리에서 시작하는 flood fill** 로 "바깥쪽에서 연결된 배경 영역"만 투명화해라. 기체 내부의 어두운 픽셀은 바깥과 연결되어 있지 않으므로 보존된다.
   - 이미지 로딩 시점(`_loadFighterImage`)에 한 번만 수행한다. 매 프레임 하지 마라.
   - `toByteData(format: ui.ImageByteFormat.rawRgba)` → 픽셀 배열 조작 → `ui.decodeImageFromPixels` 순서로 처리한다.
   - 허용 오차(색 거리)는 상수로 빼고 조정 가능하게 해라.
4. 어두운 기체를 밝게 보정한다. `Paint()..colorFilter = ColorFilter.matrix([...])` 로 밝기와 대비를 올려라. 어두운 밤하늘 배경 위에서 또렷하게 보여야 한다.

**방안 B — 에셋을 버리고 직접 그리는 방법 (벡터)**

이미 `MissileGamePainter._drawFallbackFighter()` 에 `Path` 로 전투기를 그리는 코드가 있다. 이것을 개선해서 정식 렌더링으로 승격시켜라.

- 밝은 배색: 기체는 밝은 실버/하늘색(`Color(0xFFD7E3F0)` 계열), 외곽선은 시안(`Color(0xFF4DD0E1)`), 캐노피는 밝은 하늘색, 엔진 노즐에서 나오는 화염 트레일 추가.
- 어두운 남색 하늘 배경 위에서 즉시 눈에 띌 것.
- 에셋 로딩 실패와 무관하게 항상 동작하고, 색을 마음대로 바꿀 수 있다는 장점을 살려라.

---

## 요구사항 2 — 미사일을 "동그란 총알"로, 그리고 시간에 따라 점점 많아지게

### 2-1. 모양

현재 `_drawMissile()` 은 길쭉한 미사일을 그린다 — 8×28 둥근 사각형 몸체 + 삼각 탄두 + 꼬리 화염, 그리고 진행 방향으로 `canvas.rotate(missile.angle)`.

이것을 **작고 동그란 총알(탄환)** 로 바꿔라.

- 형태: 반지름 6~8px 정도의 원.
- 스타일: 가운데 밝은 코어 + 바깥쪽 발광(`MaskFilter.blur`) + 진행 방향 반대쪽으로 짧게 늘어지는 잔상(트레일). 총알 지옥(bullet hell) 게임의 탄환처럼.
- 색: 어두운 하늘 배경과 대비되도록 밝게. 주황/노랑 계열 또는 시안 계열 중 하나로 통일하되, **플레이어 기체 색과는 확실히 구분되는 색**을 골라라.
- 원형이므로 `angle` 기반 회전은 이제 필요 없다. `Missile.angle` 이 트레일 방향에만 쓰이도록 정리하거나, `velocity` 에서 직접 계산해라.

### 2-2. 충돌 판정

미사일이 원이 되었으므로 `missileHitboxSize = Size(12, 30)` 사각형 판정은 맞지 않는다.
**원 대 사각형** 또는 **원 대 원** 판정으로 바꿔라. 총알 반지름 상수를 하나 두고 렌더링과 판정이 같은 값을 쓰게 해라. 판정 반지름은 보이는 것보다 살짝 작게 잡아 억울한 판정을 줄여라.

### 2-3. 난이도 곡선

지금은 시간 구간 4개로 값이 뚝뚝 끊긴다. 이것을 **시간에 대해 연속적으로 증가하는 곡선**으로 바꿔라. "점점 많아지는 추세"가 체감되어야 한다.

- `spawnInterval` 은 지수적으로 감소, `maxMissiles` 는 선형으로 증가시키는 식으로.
- 전체 물량을 지금보다 **확실히 늘려라**. 현재 최대 35발은 너무 적다.
- 후반에는 한 번에 1발이 아니라 2~3발씩 무리지어 스폰하는 것도 고려해라.
- 아래는 **출발점 예시**다. 그대로 쓰지 말고, 근거를 대며 조정해라. 실제 플레이 밸런스는 네가 판단해서 제안해라.

```dart
// t = elapsedTime (초)
spawnInterval = max(0.10, 1.1 * exp(-t / 38));   //  0s:1.10  30s:0.50  60s:0.23  120s:0.10
maxMissiles   = min(90,  10 + t * 1.1).toInt();  //  0s:10    30s:43    60s:76
missileSpeed  = min(420, 150 + t * 3.2);         //  0s:150   30s:246   60s:342
burstCount    = 1 + (t / 45).floor().clamp(0, 2); // 45초부터 2발, 90초부터 3발씩
```

- 미사일 수가 크게 늘어나므로 **성능을 신경 써라.** 매 프레임 `Paint` 객체를 새로 만들지 말고 재사용하고, `MaskFilter.blur` 는 비용이 크니 총알 개수가 많을 때 어떻게 할지 함께 검토해라.

---

## 요구사항 3 — 좌측 하단 조이스틱 + 우측 하단 필살기 버튼

### 3-1. 좌측 하단 가상 조이스틱

기존의 "화면 아무 데나 드래그" 방식(`GestureDetector(onPanUpdate: _handlePanUpdate)`)을 **제거하고** 조이스틱으로 대체해라. 화면 전체 드래그를 남겨두면 우측 필살기 버튼 터치와 충돌한다.

- 위치: 좌측 하단, `SafeArea` 안쪽에 여백을 두고 배치.
- 모양: 반투명 원형 베이스(지름 약 130) + 그 안에서 움직이는 작은 스틱(지름 약 56). 기존 `_glassChip` 과 같은 계열의 글래스 스타일.
- 동작:
  - 베이스 안에서 드래그하면 스틱이 따라 움직이되, 베이스 반지름을 넘지 않도록 벡터 길이를 제한(clamp)한다.
  - 손을 떼면 스틱이 중앙으로 복귀하고 기체는 멈춘다.
  - **기체 속도는 스틱을 기울인 방향과 크기에 비례**한다 (아날로그). 최대 속도 상수는 따로 둬라.
  - 기체는 화면 밖으로 나가지 않도록 `clamp` 한다 (현재 `_handlePanUpdate` 에 있는 로직 재사용).
- 조이스틱 이동 처리는 `onPanUpdate` 에서 위치를 직접 바꾸는 방식이 아니라, **정규화된 방향 벡터를 상태로 들고 게임 루프(`_updateGame`)에서 `position += dir * speed * dt`** 로 적용해라. 프레임레이트와 무관하게 일정한 속도가 나와야 한다.

### 3-2. 우측 하단 필살기 버튼

- 위치: 우측 하단, 조이스틱과 대칭.
- 모양: 반투명 원형 버튼. **보유 스톡(칸) 수를 눈에 보이게** 표시해라 (예: 버튼 둘레의 세그먼트 게이지, 또는 버튼 아래 작은 칸 아이콘 3개).
- 충전 규칙:
  - 점수가 일정량 쌓일 때마다 스톡이 **1칸씩 증가**한다. 현재 점수는 `score = (elapsedTime * 10).toInt()` 이므로 시간에 비례한다. 예: **점수 300점마다 1칸** (= 30초마다 1칸). 이 수치는 상수로 빼고, 근거와 함께 조정안을 제시해라.
  - 최대 보유 스톡은 3칸으로 제한한다.
  - 다음 칸까지의 진행도를 버튼에 게이지로 표시해서, 언제 충전되는지 알 수 있게 해라.
- 사용 규칙:
  - 스톡이 1칸 이상일 때만 누를 수 있다. 0칸이면 버튼을 흐리게(비활성) 표시.
  - 누르면 스톡 1칸을 소모하고 **3초간 무적** 상태가 된다.
  - **무적 중에는 미사일에 맞아도 `_gameOver()` 가 호출되지 않는다.** `_updateGame` 의 충돌 검사에서 무적 여부를 확인하도록 해라.
  - 무적 중 이미 화면에 있는 미사일을 어떻게 할지 함께 제안해라 (그냥 통과시키기 / 기체에 닿으면 파괴되며 터지기 / 발동 순간 화면의 미사일을 전부 제거하기). 각각의 장단점을 짧게 설명하고 하나를 추천해라.
- 무적 시각 효과 (반드시 넣어라 — 무적인지 아닌지 플레이어가 알 수 없으면 의미가 없다):
  - 기체를 감싸는 반투명 원형 실드(맥동하는 링).
  - 남은 시간을 알려주는 표시. 끝나기 0.5~1초 전에는 깜빡여서 곧 풀린다는 걸 알려라.
  - 발동 순간의 임팩트(화면 플래시, 충격파 링 등).

### 3-3. 레이아웃 주의사항

- 조이스틱과 필살기 버튼은 게임 캔버스 **위에 겹치는 오버레이**로 올린다 (`Stack`).
- 두 컨트롤이 상단 HUD(뒤로가기 버튼, 점수 표시)와 겹치지 않게 해라.
- 좁은 화면에서도 두 컨트롤이 서로 겹치거나 화면 밖으로 나가지 않는지 확인해라.
- 게임 오버 / 시작 대기 화면에서는 컨트롤을 비활성화하거나 숨겨라.

---

## 산출물 형식

1. **변경 전후를 명확히 구분한 diff 또는 교체할 코드 블록**을 줘라. `lib/main.dart` 전체를 다시 쓰지 마라. 어느 클래스의 어느 메서드/필드를 어떻게 바꾸는지 알려줘라.
2. 새로 추가하는 상수(총알 반지름, 조이스틱 크기, 최대 속도, 필살기 충전 점수, 무적 지속시간 등)는 **한곳에 모아 이름 있는 상수로** 선언해라. 코드 여기저기에 숫자를 흩뿌리지 마라.
3. **`flutter analyze` 에서 경고가 나오지 않아야 한다.** 특히 `withOpacity` 는 최신 Flutter 에서 deprecated 이니 사용을 피하거나 대체안을 써라.
4. 기존 코드 스타일(한국어 주석, `_glassChip` 글래스 UI, `CustomPainter` 직접 렌더링)을 유지해라.
5. 요구사항 1의 방안 A / 방안 B 는 둘 다 코드로 제시하고, 권장안과 그 이유를 밝혀라.
6. 밸런스 수치를 바꿨다면 **왜 그 값인지** 한 줄씩 근거를 달아라.
7. 마지막에 **내가 직접 확인해야 할 체크리스트**를 만들어라 (예: "조이스틱을 끝까지 밀었을 때 기체가 화면 밖으로 나가지 않는가", "무적 3초가 끝나면 다시 피격되는가" 등).

## 하지 말아야 할 것

- 외부 패키지 추가 금지.
- 게임을 별도 파일로 분리하지 마라. 단일 파일 구조를 유지한다.
- 다른 미니게임(토끼 탈출기, 반응속도 테스트, 1to25)의 코드는 건드리지 마라.
- 확실하지 않은 부분은 임의로 결정하지 말고, 선택지와 트레이드오프를 제시하고 물어봐라.
